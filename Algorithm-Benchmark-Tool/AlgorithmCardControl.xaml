<?xml version="1.0" encoding="utf-8"?>
    <UserControl
        x:Class="Algorithm_Benchmark_Tool.AlgorithmCardControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Algorithm_Benchmark_Tool"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

        <Border Background="#161B22" CornerRadius="8" BorderBrush="#30363D" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" MinHeight="100"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="{x:Bind AlgorithmName, Mode=OneWay}" Foreground="White" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Border Background="#363d4e" CornerRadius="4" Padding="5,2" Margin="0,0,10,0">
                        <TextBlock Text="{x:Bind Status, Mode=OneWay}" Foreground="#adc6ff" FontSize="12"/>
                    </Border>
                    <TextBlock Text="{x:Bind ElapsedMs, Mode=OneWay}" Foreground="#8B949E" FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
                </Grid>
                <Border Grid.Row="1" BorderBrush="#30363D" BorderThickness="1" CornerRadius="4" Margin="0,10" Background="#10131a">
                    <Canvas x:Name="SortingCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                </Border>

                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="COMPARISONS" Foreground="#8B949E" FontSize="10"/>
                    <TextBlock Text="{x:Bind Comparisons, Mode=OneWay}" Foreground="White" FontSize="16" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="SWAPS" Foreground="#8B949E" FontSize="10"/>
                    <TextBlock Text="{x:Bind Swaps, Mode=OneWay}" Foreground="White" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
                <Border Grid.Row="3" Margin="0,10,0,0" BorderBrush="#30363D" BorderThickness="0,1,0,0" Padding="0,10,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Độ phức tạp" Foreground="#8B949E" FontSize="10"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind Complexity, Mode=OneWay}" Foreground="#8B949E" FontSize="10" HorizontalAlignment="Right" IsHitTestVisible="True">
                            <ToolTipService.ToolTip>
                                <StackPanel Margin="5">
                                    <TextBlock Text="Độ phức tạp chi tiết:" FontWeight="Bold" Margin="0,0,0,5" FontSize="10"/>
                                    <TextBlock>
                                        <Run Text="Tốt nhất: "></Run>
                                        <Run Text="{x:Bind Best, Mode=OneWay}"></Run>
                                    </TextBlock>
                                    <TextBlock>
                                        <Run Text="Trung bình: "></Run>
                                        <Run Text="{x:Bind Avg, Mode=OneWay}"></Run>
                                    </TextBlock>
                                    <TextBlock>
                                        <Run Text="Xấu nhất: "></Run>
                                        <Run Text="{x:Bind Worst, Mode=OneWay}"></Run>
                                    </TextBlock>
                                    <TextBlock>
                                        <Run Text="Không gian: "></Run>
                                        <Run Text="{x:Bind Space, Mode=OneWay}"></Run>
                                    </TextBlock>
                                </StackPanel>
                            </ToolTipService.ToolTip>
                        </TextBlock>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </UserControl>
